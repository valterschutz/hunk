---
"hunkdiff": minor
---

Add `hunk address`, which reviews every hunk you rejected in the current repository, rebuilt from the review file with notes in place, so each can be fixed and marked addressed with `=`; `hunk address --list` prints them instead. Add `o` to open the active note's line in your editor, in a Herdr split when available.
