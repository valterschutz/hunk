---
"hunkdiff": minor
---

Add a `cursorLineBg` theme key so a custom theme can name the exact color the current line lifts toward, instead of the computed white/black tint. Unset on every bundled theme, so existing themes render unchanged.
