---
"hunkdiff": minor
---

Add the `cursor_scroll` config key. `center` keeps the current line at the middle of the viewport, so stepping with `j`/`k` and jumping between hunks scroll the review stream around it instead of only pulling the line on screen.
