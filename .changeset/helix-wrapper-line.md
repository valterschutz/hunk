---
"hunkdiff": patch
---

Recognize `helix` and wrapper scripts named `hx-wrapper` alongside `hx` when opening a file with `e`, so the selected line still carries through when `$EDITOR` points at a wrapper around Helix.
