---
"hunkdiff": patch
---

Show a hunk header's function context once. Parsed headers already carried Git's context, and Hunk appended it a second time, so a header read "@@ -46,8 +46,8 @@ main main".
