---
"hunkdiff": minor
---

Add `addedRailColor`, `removedRailColor`, and `contextRailColor` theme keys so the hunk rail marker can be recolored on its own. They default to `addedSignColor`, `removedSignColor`, and `lineNumberFg`, so existing themes render unchanged.
