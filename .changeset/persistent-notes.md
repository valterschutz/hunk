---
"hunkdiff": minor
---

Keep the reviewer's note threads in the review file. A note written beside a hunk, and every reply in its thread, is stored against the hunk's content identity and comes back in any later review that shows the same hunk, on any machine the file syncs to; a reload that drops a note no longer loses it. Deleting a note removes it from the file.
