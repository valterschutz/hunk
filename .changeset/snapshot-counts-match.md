---
"hunkdiff": patch
---

Keep the session mirror current after a reload retires a file that carries agent notes: the window now publishes a live-comment count equal to the summaries it sends, and bounds both note counts before publishing, so the daemon no longer refuses every later snapshot.
