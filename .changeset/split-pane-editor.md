---
"hunkdiff": minor
---

Add `Ctrl+E` to open the selected file in `$EDITOR` inside a new Herdr pane split below the current one, instead of suspending the review, so the diff stays visible while editing. Requires running inside a Herdr-managed pane.
