---
"hunkdiff": minor
---

Fade hunks outside the focused one so the focused hunk reads as the foreground layer. Diff tints, line numbers, signs, hunk headers, syntax colors, and word-diff emphasis all contract toward the terminal surface, stopping while unfocused code is still readable; the focused hunk keeps the theme's own colors.
