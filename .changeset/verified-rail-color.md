---
"hunkdiff": minor
---

Paint the rail of every verified hunk in the new `verifiedRailColor` theme color while verified hunks are shown, so they can be told apart from the rest of the review. Custom themes set it under `[custom_theme]`; it defaults to the added rail color.
